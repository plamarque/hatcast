# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files (never commit secrets)
.env
.env.*
!.env.example

# Recette prod locale (--with-push) : injecté depuis .env, ne pas committer
apps/web/src/environments/environment.production.local.ts

# Local HTTPS certs/keys (mkcert/dev)
*.pem
*.key
*.crt

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env.local

# Test outputs and reports
test-output/
playwright-report/
test-results/
tests/emails/

# Local Playwright configuration (contains personal IPs)
playwright.config.local.js

# Firebase cache files
.firebase/
firebase-debug.log

# Dry-run sandbox
.dry-run-sandbox/
.dry-run-sandbox-v2/

# Replay algorithm simulation output
scripts/replay/output/

# Local V1→V2 migration CSV exports (PII — do not commit)
/members.csv
/users.csv

# MIG-2/MIG-3 migration dumps + generated SQL/manifest (PII — ADR-0016, keep out of git)
/export/

# BMad standard skills (installed locally; identical copies — regenerate from BMad if needed)
.agents/skills/*
!.agents/skills/hatcast-v2-release/
!.agents/skills/hatcast-v2-release/**
.cursor/skills/

# BMad personal install answers (user_name, communication_language — per developer)
_bmad/config.user.toml

# Cursor agent skills lockfile (local pin per machine)
skills-lock.json