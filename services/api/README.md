# `services/api` — API HatCast (V2)

Emplacement du **backend V2** (cible : **Kotlin / Spring Boot** sur Cloud Run, PostgreSQL, OpenAPI — voir `_bmad-output/planning-artifacts/architecture.md`).

- Pas de code scaffoldé ici tant que les stories API ne sont pas démarrées.
- **Aucune** dépendance vers le code Vue sous [`legacy/`](../../legacy/) : contrats HTTP / DTO uniquement.
