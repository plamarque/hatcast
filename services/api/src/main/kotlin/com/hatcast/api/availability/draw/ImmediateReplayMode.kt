package com.hatcast.api.availability.draw

/** Interim draw-weight mode for immediate replay factor (story 19.9; troupe config in 19.16+). */
enum class ImmediateReplayMode {
    OFF,
    EXCLUDE,
    MALUS,
}
