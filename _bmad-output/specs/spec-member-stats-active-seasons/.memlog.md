---
topic: Unified member statistics across active current seasons
updated: 2026-09-22T08:05
---

- (direction by user) Replace newest single-season default with a unified view over all eligible current active seasons.
- (decision by user) A season is included by default only when isActive is true, the local current date is inclusively between startDate and endDate, and the player participates actively.
- (capability by user) Aggregate counts, role favorites, and events from all included seasons; distinct seasons extend the displayed interval and overlapping events remain together.
- (capability by user) Default the unified chart to the current calendar month.
- (event) Coherence validation passed: four capabilities have intent and success, default-scope constraint and non-goals are explicit.
- (event) Preservation validation passed: all user decisions on active date window, aggregation, overlapping events, and current-month focus are represented; arbitrary multi-select remains open.
- (decision by user) The season filter supports arbitrary multiple selection. The label Toutes means all eligible current active seasons, without spelling out active.
- (event) Open question on season selection resolved by user.
- (decision by user) The troupe filter supports arbitrary multiple selection or Toutes. Applying it rescopes the selectable season catalog.
- (decision by user) When a troupe selection makes selected seasons incompatible, remove those seasons; if none remain selected, fall back to Toutes, meaning all eligible current active seasons in the selected troupe scope.
- (event) Coherence validation passed after dependent multi-troupe and multi-season filter decision.
